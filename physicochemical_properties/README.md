# Physicochemical Properties

CSV-based physicochemical prediction scripts were removed.

Use the Hugging Face sidecar workflow through:

`uv run -m setup.prepare_data prepare_and_upload_physicochemical_properties --config <config-path>`

See `setup/prepare_data/README.md` for command usage and config examples.
